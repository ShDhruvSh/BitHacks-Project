#Screen Manager to switch between different screens
WindowManager:
    CameraWindow:
    ConfirmPhotoWindow:
    InfoWindow:
    ReportWindow:
    EndWindow:


#Background color for widgets
<BackgroundColor@Widget>
    background_color: 1, 1, 1, 1
    canvas.before:
        Color:
            rgba: root.background_color
        Rectangle:
            size: self.size
            pos: self.pos


#Background color for labels that are BackgroundLabels
<BackgroundLabel@Label+BackgroundColor>
    background_color: 1, 1, 1, 1

<BackgroundButton@Button+BackgroundColor>
    background_color: 1, 1, 1, 1


#First screen that allows the user to take a picture
<CameraWindow>:
    name: "main"

    FloatLayout:
        Camera:
            id: camera
            #resolution: (2000, 2000)
            play: True
            size_hint: (1.0, 0.8)
            pos_hint: {'x': 0, 'y': 0.2}

        Button:
            text: 'Take Photo'
            size_hint: (1, 0.2)
            pos_hint: {'x': 0, 'y': 0}
            on_press:
                root.onCameraClick()
                app.root.current = "second"
                root.manager.transition.direction = "left"


#Second screen that allows user to check their image and submit for processing
<ConfirmPhotoWindow>:
    name: "second"

    Button:
        text: "Verify Image"
        size_hint: (1, 0.2)
        pos_hint: {'x': 0, 'y': 0.8}
        on_release:
            root.getImage()

    Image:
        id: plate_image
        source: ""
        size_hint: (1, 0.6)
        pos_hint: {'x': 0, 'y': 0.2}

    Button:
        text: "Retake Image"
        size_hint: (0.5, 0.2)
        pos_hint: {'x': 0, 'y': 0}
        on_release:
            app.root.current = "main"
            root.manager.transition.direction = "right"
            root.replaceImage()

    Button:
        id: submit-photo
        text: "Submit Photo"
        size_hint: (0.5, 0.2)
        pos_hint: {'x': 0.5, 'y': 0}
        on_press:
            root.showPopup()


#Third window for retrieving cop info from a database
<InfoWindow>:
    name: "third"

    FloatLayout:
        Button:
            id: test-button
            text: "Get Cop Info"
            size_hint: (0.9, 0.15)
            pos_hint: {'x': 0.05, 'y': 0.84}
            halign: 'center'
            valign: 'middle'
            text_size: self.size
            font_size: 24
            on_press:
                root.getData()

        BackgroundLabel:
            id: name-label
            text: "Name: "+"" #this will be replaced with info from dictionary
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.9, 0.15)
            pos_hint: {'x': 0.05, 'y': 0.68}
            halign: 'center'
            valign: 'middle'
            text_size: self.size
            font_size: 24

        BackgroundLabel:
            text: "Badge Number: "
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.525, 0.15)
            pos_hint: {'x': 0.1, 'y': 0.52}
            halign: 'left'
            valign: 'middle'
            text_size: self.size

        BackgroundLabel:
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.05, 0.15)
            pos_hint: {'x': 0.05, 'y': 0.52}

        BackgroundLabel:
            id: id-label
            text: "" #this will be replaced with info from dictionary
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.275, 0.15)
            pos_hint: {'x': 0.675, 'y': 0.52}

        BackgroundLabel:
            text: "Official Infractions: "
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.525, 0.15)
            pos_hint: {'x': 0.1, 'y': 0.36}
            halign: 'left'
            valign: 'middle'
            text_size: self.size

        BackgroundLabel:
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.05, 0.15)
            pos_hint: {'x': 0.05, 'y': 0.36}

        BackgroundLabel:
            id: off-infrac-label
            text: "" #this will be replaced with info from dictionary
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.275, 0.15)
            pos_hint: {'x': 0.675, 'y': 0.36}

        BackgroundLabel:
            text: "Reported Infractions: "
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.525, 0.15)
            pos_hint: {'x': 0.1, 'y': 0.2}
            halign: 'left'
            valign: 'middle'
            text_size: self.size

        BackgroundLabel:
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.05, 0.15)
            pos_hint: {'x': 0.05, 'y': 0.2}

        BackgroundLabel:
            id: rep-infrac-label
            text: "" #this will be replaced with info from dictionary
            background_normal: ''
            background_color: (0, 0, 1, 1)
            size_hint: (0.275, 0.15)
            pos_hint: {'x': 0.675, 'y': 0.2}

        Button:
            text: "Take Another Photo"
            size_hint: (0.5, 0.19)
            pos_hint: {'x': 0, 'y': 0}
            on_release:
                app.root.current = "main"
                root.manager.transition.direction = "right"

        Button:
            text: "Report Cop"
            size_hint: (0.5, 0.19)
            pos_hint: {'x': 0.5, 'y': 0}
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"


#Fourth window for reporting a new infraction that a cop has made
<ReportWindow>:
    name: "fourth"

    FloatLayout:
        TextInput:
            id: report
            text: "Type out cop's infraction here"
            foreground_color: (0, 0, 0, 0.4)
            on_touch_down:
                root.clearText()
            multiline: True
            size_hint: (1.0, 0.8)
            pos_hint: {'x': 0, 'y': 0.2}

        Button:
            text: "Review Cop Information"
            size_hint: (0.5, 0.2)
            pos_hint: {'x': 0, 'y': 0}
            on_release:
                app.root.current = "third"
                root.manager.transition.direction = "right"

        Button:
            text: "Submit Report"
            size_hint: (0.5, 0.2)
            pos_hint: {'x': 0.5, 'y': 0}
            on_release:
                root.submitReport()
                app.root.current = "fifth"
                root.manager.transition.direction = "left"


#Fifth window for exiting app or taking another picture
<EndWindow>:
    name: "fifth"

    FloatLayout:
        Label:
            text: "Thank you for your report!"
            size_hint: (1.0, 0.8)
            background_color: (.3, .6, .7, 1)
            pos: (0, 100)

        Button:
            text: "Submit another report"
            size_hint: (0.5, 0.2)
            pos_hint: {'x': 0, 'y': 0}
            on_release:
                app.root.current = "main"
                root.manager.transition.direction = "right"

        Button:
            text: "Quit Application"
            size_hint: (0.5, 0.2)
            pos_hint: {'x': 0.5, 'y': 0}
            on_release:
                app.stop()